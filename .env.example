# Optional: copy to .env in the project root. Defaults work without this file.
SERVER_HOST=0.0.0.0
SERVER_PORT=3000
CLIENT_PORT=5173
# Used by the development proxy, never sent to the browser.
API_PROXY_TARGET=http://127.0.0.1:3000
